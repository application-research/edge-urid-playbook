[Unit]
Description=Edge Daemon Service

[Service]
EnvironmentFile={{ edge_main_dir }}/edge/.env
WorkingDirectory={{ edge_main_dir}}/edge
ExecStart={{ edge_binary_install_dir }}/edge daemon --repo=${REPO}
Restart=on-failure
User=estuary
Group=estuary

[Install]
WantedBy=multi-user.target
